{{ form_start(form) }}
    
    {{ form_widget(form) }}
    <button class="btn btn-outline-success bout"><i class="bi bi-check2"> </i> {{ button_label|default('sauvegarder') }}</button>
{{ form_end(form) }}
